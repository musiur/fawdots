[global]
    # Default (close_current on left-click) makes it impossible to confirm
    # actionable notifications like blueman's bluetooth pairing passkey
    # prompt — clicking just dismisses it instead of confirming, which
    # causes pairing to time out (br-connection-key-missing). Left-click
    # now triggers the notification's action (e.g. "Confirm") first.
    mouse_left_click = do_action, close_current
    mouse_middle_click = close_all
    mouse_right_click = close_current
    font = Ubuntu Nerd Font 10

# blueman's pairing prompt has two actions (confirm/deny) with no default,
# so do_action alone falls back to dunst's context menu — which may not be
# rendering usably here. Pin the action explicitly so left-click confirms
# directly. Right-click still just dismisses (implicit deny via timeout).
[bluetooth-pairing]
    appname = "Bluetooth"
    action_name = "confirm"
